$(function() {    initDate();        setInterval(function(){            setTimeout(initDate(),0)        },1000); //每隔一秒运行一次    function initDate(){        let myDate = new Date;        let year = myDate.getFullYear(); //获取当前年        let mon = myDate.getMonth() + 1; //获取当前月        let date = myDate.getDate(); //获取当前日        let h = myDate.getHours();//获取当前小时数(0-23)        let m = myDate.getMinutes();//获取当前分钟数(0-59)        let s = myDate.getSeconds();//获取当前秒        let week = myDate.getDay();        let DD, MM, hour, min;        if (date < 10) {            DD = "0" + date;        } else {            DD = date;        }        if (mon < 10) {            MM = "0" + mon;        } else {            MM = mon;        }        if (h < 10) {            hour = "0" + h;        } else {            hour = h;        }        if (m < 10) {            min = "0" + m;        } else {            min = m;        }        let weeks = ["星期日", "星期一", "星期二", "星期三", "星期四", "星期五", "星期六"];        $(".BeiJingTime").html(year + "/" + MM + "/" + DD);        if (h < 12) {            $(".ExactlyTime").html(weeks[week] + "  " + hour + ":" + min + "AM");        } else {            $(".ExactlyTime").html(weeks[week] + "  " + hour + ":" + min + "PM");        }    }})